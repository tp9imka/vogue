targets:
  $default:
    builders:
      json_serializable:
        options:
          # toJson() fully materialises nested objects into maps so a
          # toJson()/fromJson() round-trip works without jsonEncode.
          explicit_to_json: true
          field_rename: none
